# 🕸️ What is Networking? (TryHackMe)

## 🎯 Objective
Understand basic network connectivity and the fundamental use of the ping utility.

## 🧠 Core Concepts & Execution
* **Execution Environment:** Used the deployable terminal on TryHackMe.
* **Tasks Performed:**
  * Identified that the `ping` command utilizes the `ICMP` protocol.
  * Understood the proper syntax to test local connections (e.g., `ping 10.10.10.10`).
  * Executed the command `ping 8.8.8.8` to successfully reach the external DNS server and analyze the packet transmission statistics.

## 🚩 Proof of Compromise
* **Flag Found:** `THM{I_PINGED_THE_SERVER}`
*
